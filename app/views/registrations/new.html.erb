    <div class="row">
    <div class="col-md-4"></div>
    <div class="col-md-4">
        <div class="card">
            <div class="card-body">
                <h1>Sign Up</h1>
                <hr/>
                <%= form_with model: @user, url: sign_up_path do |form| %> 
                    <% if form.object.errors.any?%>                        
                        <div class="mb-3 alert alert-danger"> 
                            <% form.object.errors.full_messages.each do |message| %>
                                <div><%= message %></div>
                            <% end %>
                        </div>
                    <% end %>
                    <div class="mb-3"> 
                        <%= form.label :Name %>
                        <%= form.text_field :name, class:"form-control", placeholder: "Please enter your full Name"  %>
                    </div>
                    <div class="mb-3">
                        <%= form.label :Email %>
                        <%= form.text_field :email, class:"form-control", placeholder: "Please Enter your valid email" %>
                    </div>
                    <div class="mb-3">
                        <%= form.label :Password %>
                        <%= form.password_field :password, class:"form-control", placeholder: "Enter Password" %>
                    </div>
                    <div class="mb-3">
                        <%= form.label :Confirm %>
                        <%= form.password_field :password_confirmation, class:"form-control", placeholder: "Please confirm password" %>
                    </div>
                    <div class="mb-3"> 
                        <center><%= form.submit "Sign Up", class:"btn btn-primary" %></center>
                    </div>
                <% end %>
            </div>
        </div>
    </div>
</div>